type Query {
  company(id: String): Company!
  user(id: String): User!
}

type Mutation {
  addUser(firstName: String, age: Int, companyId: String): User
}

schema {
  query: Query
  mutation: Mutation
}
